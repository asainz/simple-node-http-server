{
  "name": "simple-node-http-server",
  "description": "Super simple node http server ready to be used with an default express app. Useful to deploy an express app to heroku.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ejs": "~1.0.0",
    "express": "~4.9.8"
  }
}
